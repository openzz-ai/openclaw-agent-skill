# 中文说明

这份 skill 用来快速复制一套 OpenClaw agent 工作系统。

核心点：

- `main` 保持 `gpt-5.4`
- 用 compaction + memory 控制上下文膨胀
- 把 OpenClaw 运维问题分流到 `openclaw-ops`
- 用 `START-HERE.md` 实现无脑操作

如果你想直接分发给别人，优先使用 `references/workspace/` + `references/openclaw-config-snippet.json`。
